// Simple CM6 decorations for Rhyme Lab Pro window.RhymeLabCM6 = (function() { "use strict"; // Simple decoration system that works without imports function createRhymeExtension() { // Return empty extension for now - will be enhanced later return []; } function setHighlights(view, ranges) { // Fallback to simple CSS class application if (!view || !ranges) return; // Clear existing highlights const existing = view.dom.querySelectorAll(".rhyme-highlight"); existing.forEach(el => el.classList.remove("rhyme-highlight")); // This is a simplified version - full CM6 integration requires proper build console.log("Rhyme highlighting ranges:", ranges); } return { createRhymeExtension: createRhymeExtension, setHighlights: setHighlights }; })();
